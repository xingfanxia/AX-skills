# .deslop-whitelist —— 本书反 AI 味/毒点/剧透扫描的豁免词（借鉴 oh-story-claudecode）
#
# 词表类确定性检测（antislop_lint / output_check 的毒点·剧透·POV 扫描）的头号误报源，
# 是【世界观术语 / 角色绰号 / 专名】恰好撞上禁用词/毒词。把它们登在这里，扫描时跳过。
#
# 放在书目录根（如 mybook/.deslop-whitelist），脚本用 --whitelist mybook/.deslop-whitelist 加载。
# 规则：一行一词；# 开头为注释；空行忽略；首尾空白 trim。
# 匹配：若命中片段【是某白名单词的真子串、或本身在白名单】→ 跳过该次告警。
#
# ---- 示例（删掉换成你自己的）----
缓缓              # 主角绰号"缓缓"，不是禁用副词
仿佛山海          # 功法名（含"仿佛"但是专名）
深渊之眼          # 地名
血契残片          # 设定专名（若它在本卷已可公开、不再是剧透，可豁免剧透-出扫描）
